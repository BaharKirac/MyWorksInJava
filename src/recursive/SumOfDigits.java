package recursive;
/*
public class SumOfDigits {

    // Recursive method to calculate sum of digits
    public static int sumOfDigits(int n) {
        if (n == 0) return 0;                       // Base case: when n reduces to 0
        return (n % 10) + sumOfDigits(n / 10);      // Recursive case: last digit + remaining digits
    }
    public static void main(String[] args) {
        int number = 12345;  // Example number (can be changed in exam)
        System.out.println("Sum of digits of " + number + " is: " + sumOfDigits(number));
    }
}
*/